<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_search"
        android:title="@string/action_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView"
        android:visible="false"/>

    <!-- Filter Events -->
    <item android:id="@+id/action_filter" android:title="@string/action_filter"
        android:icon="@android:drawable/ic_dialog_dialer"
        app:showAsAction="always"
        android:visible="false" />

    <item android:id="@+id/action_set_en"
        android:title="English" app:showAsAction="never" />
    <item android:id="@+id/action_set_de"
        android:title="Deutsch" app:showAsAction="never" />
    <item android:id="@+id/action_logout"
        android:title="Log Out" app:showAsAction="never" />
</menu>
